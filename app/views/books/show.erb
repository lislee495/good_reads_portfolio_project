
<% end %>
  <h1><%=@book.name%></h1>

  <h3><%=@book.author.name%></h3>
  <br>
  <div class="btn-group">
    <% if @book.owner == @user %>
    <a class="btn btn-primary mx-2" href="/books/<%=@book.slug%>/edit" role="button">Update</a>
    <%end%>
    <% if !@user.books.include?(@book) %>
      <a class="btn btn-primary mx-2" href="/books/<%=@book.slug%>/add" role="button">Add to My Books</a>
    <% end %>
  </div>
